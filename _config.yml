# GitHub Pages configuration
theme: jekyll-theme-slate

# Ensure XML and other files are included
includes:
  - sitemap.xml
  - robots.txt

# Keep files with these extensions
keep_files:
  - sitemap.xml
  - robots.txt
  - favicon.ico

# Build settings
markdown: kramdown
theme: jekyll-theme-slate
